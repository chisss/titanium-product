# Product 模块设计说明

## 设计目标

维护保险产品、版本、条款关系及价格定义，并通过费率表、费用项、计算模型和定价包，为出单与保全流程提供可追溯的保费试算、确认计算和版本化差额报价能力。

## 方案选择

- Product 负责价格定义，Channel 负责渠道合同和佣金方案，Billing 负责客户应收、佣金应付和账务勾稽。
- 试算不落账；确认计算生成不可变计算事实，供 Policy/Billing 复核。
- 通过 Feature Center 解析特征、Rule Engine 执行版本化规则，Product 通过 Port 隔离远程适配器。

## 关键决策

- 定价包、费率表、费用项、计算模型和规则工件均按版本发布，并在计算结果中记录版本证据。
- 税费策略作为独立版本化聚合维护税基、司法辖区、法规依据、价内/价外模式和免税特征；定价包按编码、版本和内容 hash 精确引用，禁止用自由 JSON 代替。
- 后台使用“定价包”作为业务名称；`PricingPlan` Java 类名和 V1 API 首期保持兼容。
- 计算模型使用结构化节点和依赖边表达 DAG，发布前校验引用、拓扑和内容 hash，已发布版本不可原地修改。
- `QUOTE` 仅输出客户可见费用；`CONFIRM` 保存客户价格、内部成本、汇总及版本证据，二者复用同一确定性执行器。
- 同一产品允许按 ISO 4217 币种发布并行生效的定价包；有效期冲突和在线方案选择均在币种内判断。
- 计算接口要求租户、产品版本、业务时间和幂等请求号，失败时拒绝返回不完整结果。
- V2-D1 使用 `MAINTENANCE` 生成替代计算，Product 比较原确认计算并保存不可变生命周期差额；客户应付、税费和内部成本分别汇总，差额行保留费用项语义。
- 旧产品未配置定价计划时保留 RATE_TABLE 兼容路径；非费率产品没有已发布计划时拒绝计算。
- Product 负责版本化保全 Offering，Maintenance 负责保全项定义；Offering 发布必须引用同产品版本的已发布 PricingPlan，同一版本组合的生效区间不得重叠。
- M4-05 保全报价复用 `MAINTENANCE` 确认计算和生命周期差额事实，要求精确产品/计划版本及完整载荷 SHA-256；`quoteVersion` 等于差额事实 `resultHash`，有效期固定为创建后 24 小时，Product 不调用 Billing/Payment。

## 安全边界

- 所有查询和计算按 tenantId 隔离；远程 Feature Center/Rule Engine 响应需校验身份和版本。
- 费用明细由 `product:actuarial` 权限保护；租户可分别配置内部金额和内部字段脱敏，客户价格不得被内部脱敏策略修改。
- 后台产品列表和产品明细都必须把 tenantId 作为数据库查询条件，不以请求头存在代替数据过滤。
- Underwriting 按产品编码获取核保配置，Product 在租户范围内解析编码，禁止把编码误作产品ID查询。
- 管理端发布能力必须经认证授权；生产环境不得使用 Docker 验收配置中的安全排除项。
- Offering 正式解析只接受受控产品 ID、产品/计划版本、状态、渠道和业务时点，不接收任意远程地址；Repository 使用参数化 JPA 查询并以 tenantId 为首个过滤条件，响应由 Maintenance 再校验租户、产品、版本和 SHA-256 证据。
- 保全报价接口只接受固定产品路径和结构化定价输入，服务端复算完整载荷摘要并校验原确认计算的产品、币种及期望计划版本；任何摘要或版本不一致均失败关闭，不保存替代计算。
- 当前 Offering 后台沿用 Product 网关身份头信任边界，尚未新增 Offering 级 Spring Security 权限和操作人审计字段；生产开放管理路由前必须由网关限制产品配置角色，后续补齐 `product:maintenance-offering:manage` 权限。

## 已知限制

- 当前规则工件以固定版本调用，尚未支持在线脚本热更新。
- V2-B 已支持税费、印花税和监管附加费的比例计费、价内/价外税及特征免税；尚未支持阶梯税、封顶/保底税、复合税和汇率模型。
- M4-05 已覆盖批改/退保意图的版本化差额报价，但 Policy 尚未自动提供原确认计算引用；Billing 入账、真实收退款、佣金回拨和后台精算明细仍属于后续工作包。

## 变更历史

### 2026-08-27 - 支持链式保全差额计算

**变更内容**: 生命周期差额的原计算允许使用出单确认事实或上一笔保全计算事实，替代计算仍强制为 `MAINTENANCE`。

**变更理由**: 连续保全必须以当前合同对应的最近计算事实为基准，否则第二笔及后续有费用保全无法报价。

**影响范围**: Product 生命周期差额领域规则及连续保全报价；首次保全和出单确认计算保持兼容。

### 2026-08-25 - 完成 M4-05 Product 版本化报价

**变更内容**: 新增正式保全报价 API，按冻结快照、产品/计划版本和完整定价输入生成 `MAINTENANCE` 替代计算及生命周期差额，返回原/替代计算、版本证据、方向、金额、摘要和 24 小时有效期。

**变更理由**: 保全域只能编排和保存检查点，最终金额、方向和报价版本必须由持有价格定义的 Product 形成，禁止调用方自报。

**影响范围**: Product API/Application/Web 及定向路由、版本门禁和兼容性测试；Billing/Payment 未接入。

### 2026-08-24 - 完成 M3-03 Product 保全 Offering

**变更内容**: 新增版本化 Offering 聚合、独立 JPA 持久化、后台创建/发布/查询接口和正式跨域解析 API，返回允许保全项及版本/hash 证据。

**变更理由**: 产品适用性属于 Product，不能由 Maintenance Adapter 使用本地默认清单或从保全项定义反推。

**影响范围**: Product common/domain/application/infrastructure/api/web、Liquibase，以及 Maintenance 建案适用性编排。

### 2026-08-20 - 完成 V2-D1 生命周期费用差额切片

**变更内容**: 新增 `MAINTENANCE` 确认计算用途、不可变生命周期差额聚合、结构化头行持久化及 Product API，区分客户应付、税费和内部成本方向与金额。

**变更理由**: 批改、续期和退保不能覆盖原确认计算，必须以原/替代两份不可变事实生成可追溯差额。

**影响范围**: Product common/domain/application/infrastructure/api/web、Liquibase、Docker E2E。

**决策依据**: Product 负责计算价格和差额，Billing 只回查事实并登记余额，不复制保险产品公式。

### 2026-08-20 - 完成 V2-B 税费与完整应收切片

**变更内容**: 新增版本化税费策略、定价包税费策略精确引用、价内/价外及免税计算证据，并向 Billing 输出可核对的客户应收和税务事实。

**变更理由**: 将税费从自由扩展字段提升为可发布、可追溯的产品价格定义，并保证出单确认金额可由账务域完整承接。

**影响范围**: Product domain/application/infrastructure/web、Liquibase、Admin 精算工作台及 Billing 计算事实接口。

**决策依据**: Product 持有税费公式与版本事实，Billing 只校验和入账，不复制产品计价逻辑。

### 2026-08-19 - 补齐费用计算能力说明

记录 Product 定价配置、Feature Center、Rule Engine 和 Billing 的边界，支撑出单验收。

### 2026-08-19 - 完成 V2-A 精算费用计算切片

**变更内容**: 新增版本化费用项、计算模型 DAG、定价包引用、结构化试算/确认结果和精算管理 API。

**变更理由**: 将单一总保费升级为可解释、可追溯且可扩展的费用组成，为税费、佣金和账务承接提供稳定事实。

**影响范围**: Product domain/application/infrastructure/web、Liquibase、Admin 代理和精算后台。

**决策依据**: Product 管价格定义，Channel 管渠道合同，Billing 管客户应收和佣金应付；运行接口保持向后兼容。

### 2026-08-19 - 修复核保配置查询契约

新增按产品编码查询核保配置的内部 API，并将产品明细查询收敛为租户范围查询和稳定 404。

### 2026-08-19 - 定价包按币种隔离

发布冲突校验和在线有效定价包查询加入币种条件，使同一产品可并行维护多个 ISO 4217 币种版本。
